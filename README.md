# Machine Learning Workspace

This repository contains my machine learning projects, data processing scripts, and Jupyter notebooks. 

## Projects Index

As I add new projects, I will link them here for quick navigation:

* **[Credit Card Fraud Detection](Credit_Card_fraud.ipynb)**: Fraud detection and anomaly identification on a highly imbalanced dataset.
* *(More projects will be added here)*

## Setup & Execution

Most of these projects rely on the standard Python data science stack. It is highly recommended to run them in an isolated virtual environment.

```bash
# Create and activate a virtual environment
python -m venv venv
source venv/bin/activate  # Use `venv\Scripts\activate` on Windows

# Install dependencies (if a requirements.txt is provided in the specific project folder)
pip install -r requirements.txt
